<div class="container-home">
  <div class="banner">
    <%= image_tag "home_banner_main.jpg", alt: "Image landing page" %>
  </div>
  <div class="home-searchform">
    <%= form_tag donations_path, method: 'GET', html: { class: 'form-inline' } do %>
      <%= label_tag :query do %>
      <%end%>
      <%= text_field_tag :query,
      params[:query],
      class: "form-inline",
      placeholder: "What are you looking for?"
      %>
      <%= text_field_tag :address,
      params[:address],
      class: "form-inline",
      placeholder: "Approximate location"
      %>
      <%= submit_tag "SEARCH", class: "button-filled-search" %>
      <%= submit_tag "SHOW ALL", class: "button-filled-list-all" %>
    <% end %>
  </div>
  <div class="card-home">
    <h2>Help homeless in your <br>neighbourhood</h2>
      <p>You have some spare clothes or equipment that are still in good condition?
        Or you prepared too much food for an event? <br><br>You could be connected with a person in need, directly in your community.
      </p>
    </div>
    <div id="homepage-btn">
      <%= link_to "ADD DONATION", new_donation_path, class: "button-filled-add" %>
    </div>
    <div id="homepage-btn">
      <% if current_user %>
        <%= link_to "MY DONATIONS", user_path(current_user), class: "button-filled-add" %>
      <% end %>
    </div>
    <div class="card-home">
      <h2>Among people.<br>Homeless people<br>and their stories</h2>
        <p>There are nearly 2000 people in Berlin currently without a home.
          With the support of homeless organisations and the local community, they've achieved amazing things and overcome some huge obstacles. <br>
          <br>Here are some of the real stories of Berlin's homeless and volunteers.
        </p>
    </div>
    <div id="homepage-btn">
      <%= link_to "STORIES", new_donation_path, class: "button-filled-stories" %>
    </div>
      <div class="footer-element">
    <%= image_tag "home_footer_element.jpg", alt: "Footer element home page" %>
      </div>
    </div>
</div>
